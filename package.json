{
	"name": "@picobase/repo",
	"private": true,
	"scripts": {
		"dev": "pnpm -r dev",
		"start": "pnpm -r start",
		"seed": "pnpm -r seed",
		"format": "biome format --write .",
		"check": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.4"
	}
}
